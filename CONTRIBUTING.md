# Contributing

Keep collection focused on ingestion and raw gateway behavior.

- Preserve source-native fields and provenance.
- Prefer explicit importers for background datasets.
- Add tests for parsing, normalization, and database contracts when behavior changes.
- Run `make quality` before submitting changes.

For source additions, document credentials, upstream terms, rate limits, and the
exact fields used for stable IDs and timestamps.
